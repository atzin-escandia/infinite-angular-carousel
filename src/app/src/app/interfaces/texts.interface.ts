export interface TextInterface {
  active: boolean;
  activeMD: boolean;
  activeVideo: boolean;
  createdAt: string;
  id: string;
  updatedAt: string;
  _id: string;
}

export type TextsInterface = [TextInterface];
