import { PROJECT_TYPE } from '../../../../constants/landing.constants';

export const PROJECT_TRANSLATION: Map<string, string> = new Map([
  [PROJECT_TYPE.ADOPTIONS, 'adoption'],
  [PROJECT_TYPE.BOXES, 'oh'],
]);
